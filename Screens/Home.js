import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Home = () => {
  return (
    <View style={{flex: 1, backgroundColor: "#fff", justifyContent: 'center', alignItems: 'center', borderWidth: 1}} >
      <Text>This is Home Page</Text>
    </View>
  )
}

export default Home

const styles = StyleSheet.create({})